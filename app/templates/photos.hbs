<div class="container">
  <form>
    <div class="form-group">
      <label class="control-label">Select Photo</label>
      <input id="inputFile" type="file" class="file">
      <button {{action 'uploadPhoto'}}>Upload</button>
      <button {{action 'safariAlertTest'}}>Safari Alert Test</button>
      <button {{action 'testFunc'}}>Test</button>
      <button {{action 'stressTest'}}>Stress Test</button>
      <button {{action 'querySpace'}}>Query Space</button>
      <div id="img_meta_data"></div>
    </div>
  </form>
</div>
